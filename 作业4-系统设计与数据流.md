# 作业4：系统设计与数据流（美妆零售知识库与客服协作）

> 对应 `作业4.md` 交付要求：系统架构、数据来源和去向、输入输出字段、现有系统接入方式、MCP/API/CLI 选择依据、安全与兜底。
>
> 设计参考已安装 skill：`api-and-interface-design`（接口边界）、`postgres-patterns`（范式/schema）、`backend-patterns`（服务层解耦）、`mcp-builder`（MCP 接入）、`security-and-hardening`（鉴权/输入校验/权限边界）。

---

## 一、系统架构

### 1.1 角色与模块（6 方）

```
┌──────────┐   question    ┌──────────┐  query   ┌──────────────────┐
│ A 用户   │ ────────────► │ B Agent  │ ───────► │ C 企业CLI/MCP层  │
│ (消费者) │ ◄──────────── │  (AI)    │ ◄─────── │ (封装飞书检索)   │
└──────────┘ answer+cite   └────┬─────┘  chunks  └────────┬─────────┘
                                │                          │ 只读
                                │ risk=高/无覆盖           ▼
                                ▼                  ┌──────────────────┐
                        ┌──────────┐  工单        │ 飞书企业系统     │
                        │ D 初级   │ ◄──────────  │ · 5篇产品云文档  │
                        │   客服   │ ─────────►   │ · 多维表格(商品/ │
                        └────┬─────┘  升级         │   效期/会话标记) │
                             │                    └──────────────────┘
                             ▼ 升级                  ▲ 标记写入
                        ┌──────────┐  研判           │
                        │ E 资深   │ ───────────────►│ F 会话标记库
                        │   员工   │ ─► 回复用户      │ (只增不改)
                        └──────────┘                 │
                                                     │ 检索复用
                             ◄────────────────────────┘
```

### 1.2 模块依赖链（解耦）

```
知识库(飞书) ──► 检索层(CLI/MCP) ──► 回答生成 ──► 风险分级 ──► 转人工 ──► 会话标记
   只读          标准化片段输出      答案+依据      低/中/高      两级流转    留档可检索
```

**解耦原则**（参考 `backend-patterns` 服务层拆分）：
- Agent 不直连飞书，只调 CLI/MCP 层 → 飞书细节变更不影响 Agent 逻辑
- 检索层只返回结构化片段，不生成答案 → 检索与生成职责分离
- 知识库只读，会话标记单独建库 → 原始知识不可污染，经验沉淀隔离

---

## 二、数据来源和去向

| 数据 | 来源（数据从哪来） | 去向（被谁读/写） | 形态 | 飞书位置 |
|------|------|------|------|------|
| 产品知识 | 飞书云文档 ×5 | Agent 检索（只读） | 长文本 Markdown | 文件夹 `K64mfxVq5lxVKXdDynicTwWcn5g` |
| 商品信息 | 多维表格·商品信息表 | Agent 查询（只读） | 结构化字段 | Base `W6SAbbkXFaOUV1sWCqAcxl2ynSQ` / `tblEoHP6EPL63Wmy` |
| 库存效期 | 多维表格·库存效期表 | Agent 查询（只读） | 结构化字段 | `tblicHw9gxC2nS4M` |
| 会话标记 | 资深员工写入 | 资深员工/Agent 检索（只增不改） | case 记录 | `tblpRpbprhZIzlqp` |
| 离线兜底 | 本地 `知识库/*.md` | Agent（飞书不可达时） | Markdown | 项目仓库 |

> Base 地址：https://guanghe-studio.feishu.cn/base/W6SAbbkXFaOUV1sWCqAcxl2ynSQ

---

## 三、输入输出字段（各方接口契约）

> 参考 `api-and-interface-design`：每个模块边界用明确字段契约定义，避免隐式依赖。

### A 用户 → B Agent
```ts
// 用户输出（给 Agent）
question: string                      // 原始问题
user_ctx?: { skin_type?, pregnancy?, allergy?, age? }  // 可选上下文
```

### B Agent 输出（给用户/客服）
```ts
answer: string                        // 生成的回答
citations: [{ doc_id, doc_title, section, quote, url }]  // 依据，每条可溯源
risk_level: "低" | "中" | "高"
need_human: boolean                   // 是否建议转人工
transfer_hint?: string                // 转人工提示语
```

### B Agent ↔ C 检索层（CLI/MCP）
```ts
// Agent → 检索层
query: string
filters?: { brand?, category?, product_id? }
source?: "doc" | "base" | "all"       // 指定查云文档/多维表格/全部

// 检索层 → Agent
chunks: [{ doc_id, title, section, position, quote, url }]
coverage: boolean                     // 是否有匹配（false→建议转人工）
source_type: "云文档" | "多维表格" | "会话库"
```

### D 初级客服
```ts
// 输入工单
{ question, ai_answer, citations, risk_level, user_ctx }
// 输出
reply: string          // 能处理则直接回复
| escalate: boolean    // 不能处理则升级
```

### E 资深员工 → F 会话标记库
```ts
case_id: string
question: string
risk_level: "低" | "中" | "高"
resolution: string                     // 最终处理结果
handler: string                        // 处理人
tags: string[]                         // 标签（便于后续检索）
created_at: datetime
```

---

## 四、现有系统接入方式

用**飞书 CLI（lark-cli）** 封装企业系统数据交互，模拟真实企业系统接入：

| 业务动作 | 接入命令 | 权限 scope |
|---------|---------|-----------|
| 检索产品知识云文档 | `lark-cli doc` / `drive +preview` 读 5 篇 docx | doc:read |
| 查商品信息 | `lark-cli base +record-search` 商品信息表 | base:record:read |
| 查库存效期 | `lark-cli base +record-search` 库存效期表 | base:record:read |
| 标记会话留档 | `lark-cli base +record-batch-create` 会话标记表 | base:record:create |
| 检索历史案例 | `lark-cli base +record-search` 会话标记表 | base:record:read |

**身份**：user 身份（吴绍恒），已授予 base 全套 scope；知识库只读，仅会话标记表可写。

---

## 五、MCP / API / CLI 选择依据

| 方式 | 是否采用 | 选择依据 |
|------|:---:|------|
| **CLI** | ✅ 本期主交付 | 作业4 明确要 CLI Demo；`python agent.py ask "..."` 直接验证业务流程，开发快、无部署负担 |
| **MCP** | ✅ 企业系统接入层 | 把"飞书检索 + 多维表格查询"封装为 MCP server，Agent 通过标准 MCP 工具调用——这是"现有系统接入方式"的落点。参考 `mcp-builder`：MCP 天然解耦（Agent 不感知飞书细节），工具契约即接口边界，未来换数据源只改 server 不改 Agent |
| **API (REST)** | ⏸ 后续扩展 | 架构上输入输出已标准化（见第三节），后续升级 Web 时用 Django 封装 `/api/ask` 即可，本期不做 |

> **为何 CLI 优先、MCP 作接入层**：CLI 满足作业验收的最短路径；MCP 解决"如何接入企业现有系统"这一核心命题，且不引入前端/部署复杂度。REST 留作扩展，避免本期过度设计。

---

## 六、范式设计（1NF / 2NF / 3NF）

> 参考 `postgres-patterns`：把知识库当关系数据库设计，消除冗余与传递依赖。多维表格的字段划分遵循此原则。

### 6.1 三范式应用

| 范式 | 要求 | 本设计落地 |
|------|------|------|
| **1NF 原子性** | 字段不可再分 | 成分不塞逗号串→拆成成分表；citation 拆成 `{doc_id, section, quote}` 结构 |
| **2NF 完全依赖** | 非键字段完全依赖主键 | 产品↔成分多对多→`product_ingredient` 关联表（含浓度），不在产品主表塞成分 |
| **3NF 消除传递** | 消除传递依赖 | 品牌信息（制造商/产地）单独 `brand` 表，产品表只存 `brand_id`，不在产品表重复存制造商名 |

### 6.2 核心表结构

```
product(product_id PK, name, brand_id FK, category, spec, price, risk_default, source_url, doc_url)
brand(brand_id PK, name, manufacturer, country)
ingredient(ingredient_id PK, name_en, name_zh, function)
product_ingredient(product_id FK, ingredient_id FK, concentration, role)  -- 多对多
risk_rule(rule_id PK, trigger_keyword, level, action)
session_mark(case_id PK, question, risk_level, resolution, handler, tags, created_at)
```

### 6.3 飞书多维表格的对应实现

本期多维表格为"企业系统模拟"，按解耦与范式思路拆 3 表：
- **商品信息表**：product 主信息（brand 冗余为文本字段，Demo 阶段不强制外键）
- **库存效期表**：商品 + 批次 + 效期（按批次维度记录，与商品信息分离，符合 2NF——库存数量依赖"批次"而非"产品"）
- **会话标记表**：session_mark（独立于知识库，只增不改）

> 完整成分级关联（product_ingredient）在 Demo 阶段以云文档内"关键成分表"承载，后续如需结构化检索可再建成分表。

---

## 七、安全与人工兜底

> 作业4 说明：数据安全和日志审计不硬性要求，聚焦跑通业务流程。以下点到即止。

| 维度 | 设计 |
|------|------|
| **身份认证** | 飞书 user/bot 双身份；CLI 调用走 user 身份 OAuth，token 存 keyring |
| **最小权限** | 知识库（云文档+商品/效期表）只读；仅会话标记表可写；bot 未授 base 写权限 |
| **敏感数据** | 成分/孕期/过敏相关回答不外传个人画像；用户上下文 user_ctx 仅本次会话用，不落库 |
| **密钥管理** | 飞书 token / LLM API key 走环境变量与 keyring，不进仓库（.gitignore 已排除） |
| **异常处理** | 检索为空→coverage=false→建议转人工；飞书不可达→本地知识库兜底；LLM 超时→返回兜底提示 |
| **人工兜底** | 两级转人工：①AI 标记高风险→建议用户转人工 ②初级客服无法处理→转资深员工；资深员工研判后标记留档 |
| **幻觉防线** | 每条答案绑定 citations 原文出处；知识库无覆盖不硬答（模板 B/C）；高风险宁可多拦（过拦截率容忍 ≤10%） |

---

## 八、与已建飞书资源的映射

| 本设计要素 | 飞书实体 | 标识 |
|------|------|------|
| 产品知识库 | 5 篇云文档 | 文件夹 `K64mfxVq5lxVKXdDynicTwWcn5g` |
| 商品信息 | 多维表格·商品信息表 | `tblEoHP6EPL63Wmy`（5 条产品） |
| 库存效期 | 多维表格·库存效期表 | `tblicHw9gxC2nS4M`（5 条） |
| 会话标记库 | 多维表格·会话标记表 | `tblpRpbprhZIzlqp`（待填） |
| 企业系统总库 | 多维表格 | app_token `W6SAbbkXFaOUV1sWCqAcxl2ynSQ` |

---

## 九、下一步

1. **实现检索层**：用 lark-cli 封装一个 `enterprise_kb` MCP server 或 Python CLI 子命令，提供 `search_product_docs` / `query_inventory` / `mark_session` 三个工具
2. **打通 Agent**：让 `beauty-retail-answer` skill 的检索步骤改为调用上述工具，本地 `知识库/` 作兜底
3. **端到端 Demo**：`python agent.py ask "珀莱雅防晒孕期可以用吗"` → 检索飞书→定级(高)→输出答案+依据+建议转人工
