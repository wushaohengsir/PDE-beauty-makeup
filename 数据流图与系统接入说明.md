# 数据流图与系统接入说明

项目： 美妆零售知识库与客服协作系统
赛道： 美妆零售知识检索与合规问答
日期： 2026-08-02
作者： 吴绍恒

> 本文件为飞书云文档 `W5B8d7bkWoELpwxEduuchW6enOd` 的本地副本，飞书版为权威版本（表格渲染更佳）。
> 飞书链接：https://guanghe-studio.feishu.cn/docx/W5B8d7bkWoELpwxEduuchW6enOd

---

## 一、系统架构概览

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│            美妆零售知识库与客服协作系统 — 架构                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐         │
│  │  消费者端    │   │  客服工作台  │   │  资深员工端  │         │
│  │  (CLI 调用)  │   │ (Agent辅助)  │   │ (研判+标记)  │         │
│  └──────┬───────┘   └──────┬───────┘   └──────┬───────┘         │
│         └──────────────────┼──────────────────┘                 │
│                            ▼                                    │
│         ┌──────────────────────────────────────┐                │
│         │   Agent 核心编排层                    │                │
│         │   (beauty-retail-answer skill)       │                │
│         │   检索 → 风险定级 → 生成答案+依据     │                │
│         └──────────────────┬───────────────────┘                │
│                            │ query/chunks                       │
│                            ▼                                    │
│         ┌──────────────────────────────────────┐                │
│         │   企业系统接入层 (lark-cli 封装)       │                │
│         │   检索云文档 / 查多维表格 / 写会话标记 │                │
│         └──────────────────┬───────────────────┘                │
│                            ▼ 只读                               │
│         ┌──────────────────────────────────────┐                │
│         │   飞书企业系统 (模拟)                  │                │
│         │   · 产品知识云文档 (持续扩充)          │                │
│         │   · 多维表格:商品信息/库存效期/会话标记│                │
│         └──────────────────────────────────────┘                │
│                                                                 │
│   离线兜底: 本地 知识库/*.md (飞书不可达时)                       │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 技术选型说明

| 层面 | 选型 | 理由 |
|------|------|------|
| Agent 编排 | beauty-retail-answer skill（已实现） | 检索→定级→模板输出→转人工，链路成熟 |
| 企业系统接入 | lark-cli（飞书 CLI） | 直接读写飞书云文档与多维表格，模拟真实企业系统数据交互 |
| 知识库形态 | 飞书云文档 + 多维表格 | 长文本知识用云文档，结构化商品/效期/会话用多维表格 |
| 本期交付 | CLI | `python agent.py ask "..."` 验证业务流程，无部署负担 |
| 兜底 | 本地 Markdown | 飞书不可达时离线检索 |

---

## 二、数据流图（Data Flow Diagram）

### 2.1 核心数据流：消费者咨询 → Agent 回答（低风险路径）

```
消费者提问
   │ question, user_ctx?
   ▼
Agent 理解问题（提取产品名/成分/肤质/诉求）
   │ query, filters
   ▼
企业接入层检索飞书 ──► 产品知识云文档（语义匹配）
   │                 └► 多维表格·商品信息/库存效期（结构化查询）
   │ chunks, coverage
   ▼
Agent 风险定级（低/中/高，灰区取高）
   │
   ├─ 低风险 ──► 生成答案 + citations 依据 ──► 返回消费者
   │                                            │
   └─ 中风险 ──► 生成答案 + 依据 + 额外提醒 ──► 返回消费者
                                                ▼
                                        消费者自行决定采纳
```

### 2.2 高风险升级数据流：不良反应/敏感问题 → 工单创建

```
消费者提问（孕期/过敏/医美/药物/儿童 等）
   │
   ▼
Agent 检索 + 风险定级 = 高风险
   │
   ▼
生成答案 + 依据 + ⚠建议转人工（need_human=true）
   │
   ▼
消费者采纳转人工 ──► 创建工单（写入多维表格·会话标记表）
   │                  case_id, question, risk_level, ...
   ▼
初级客服接单
   ├─ 能处理 ──► 回复消费者
   └─ 不能处理 ──► 转资深员工
                      │
                      ▼
               资深员工研判 ──► 回复消费者
                      │
                      └─► 标记会话留档（更新会话标记表：resolution, handler, tags）
                                  │
                                  └─► 后续同类问题可检索复用
```

### 2.3 数据字段映射表

#### 输入字段（消费者/员工 → Agent）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| question | string | 是 | 用户原始问题 |
| user_ctx.skin_type | enum | 否 | 干/油/混/敏感 |
| user_ctx.pregnancy | bool | 否 | 是否孕期/哺乳期 |
| user_ctx.allergy | string[] | 否 | 已知过敏成分 |
| user_ctx.age | string | 否 | 年龄段 |

#### 输出字段（Agent → 消费者/员工）

| 字段 | 类型 | 说明 |
|------|------|------|
| answer | string | 生成的回答（结论在前细节在后） |
| citations | array | 每条含 doc_id, doc_title, section, quote, url |
| risk_level | enum | 低 / 中 / 高 |
| need_human | bool | 是否建议转人工 |
| transfer_hint | string? | 转人工提示语（高风险时） |

#### 飞书知识库（云文档）↔ Agent 字段

| 方向 | 字段 | 说明 |
|------|------|------|
| Agent → 云文档 | query, doc_token? | 按关键词检索，可指定文档 |
| 云文档 → Agent | doc_id, title, section, quote, url | 返回匹配段落与原文位置 |

#### 飞书多维表格 ↔ Agent 字段

| 表 | 方向 | 关键字段 |
|------|------|------|
| 商品信息 | 读 | product_id, 产品名称, 品牌, 品类, 规格, 风险等级默认, 飞书文档链接 |
| 库存效期 | 读 | 批次号, 商品, 库存数量, 生产日期, 有效期至, 状态(正常/临期/过期) |
| 会话标记 | 写 | case_id, 用户问题, 风险等级, 处理结果, 处理人, 标签, 时间 |
| 会话标记 | 读 | 同上（检索历史案例复用） |

---

## 三、系统接入方式与选择依据

### 3.1 接入方式对比：MCP / API / CLI

| 方式 | 实现成本 | token 开销 | 验收难度 | 适合场景 |
|------|:---:|:---:|:---:|------|
| CLI | 低 | 小 | 易 | Demo / 业务跑通；按需执行命令 |
| MCP | 中 | 大 | 中 | MCP 原生客户端直接调用（工具 schema 注入上下文） |
| REST API | 高 | 中 | 难 | 对接前端 / 企业系统正式集成 |

> token 开销差异：MCP 会把所有工具的结构定义（schema）注入 LLM 上下文，飞书官方 MCP 暴露整个 OpenAPI、工具数量多，占用 token 较大；CLI 按需执行单条命令，token 开销更小。

### 3.2 本项目接入方式选择

**本期采用 CLI（lark-cli）。**

- **能力覆盖**：lark-cli 能读写飞书云文档与多维表格、写入会话标记，已覆盖本期全部业务需求（检索产品知识、查商品/效期、标记留档），无需引入其他接入方式。
- **token 开销更小**：相比 MCP 把大量工具 schema 注入上下文，CLI 按需执行命令，token 占用更少，更适合 Demo 阶段的轻量调用。
- **REST API（后续扩展）**：架构上输入输出已标准化（见 2.3），后续升级 Web 时用 Django 封装 `/api/ask` 即可，本期不做。

---

## 四、运行证据

> 以下为本次在飞书（租户 guanghe-studio.feishu.cn，user 身份 吴绍恒）实际执行的命令与结果。知识库文档数量会持续扩充，下方为首批已导入内容。

### 4.1 证据一：创建多维表格（模拟企业系统数据库）

```
lark-cli base +base-create --name "美妆零售企业系统数据库" \
  --table-name "商品信息" --fields '[...10个字段...]'
```

结果：Base 创建成功，app_token = `W6SAbbkXFaOUV1sWCqAcxl2ynSQ`
链接：https://guanghe-studio.feishu.cn/base/W6SAbbkXFaOUV1sWCqAcxl2ynSQ

随后创建 3 张表：
| 表名 | table_id | 用途 |
|------|------|------|
| 商品信息 | tblEoHP6EPL63Wmy | 产品主数据 |
| 库存效期 | tblicHw9gxC2nS4M | 批次/效期/库存 |
| 会话标记 | tblpRpbprhZIzlqp | 工单留档 |

### 4.2 证据二：向多维表格写入数据

```
lark-cli base +record-batch-create --base-token W6SAbbkXFaOUV1sWCqAcxl2ynSQ \
  --table-id tblEoHP6EPL63Wmy --json '{"create_records":[...产品记录...]}'
```

写入结果：
- 商品信息表：首批产品记录（α-熊果苷、烟酰胺、宝拉2%BHA、珀莱雅防晒、CATHERINEY卸妆膏，后续持续扩充）
- 库存效期表：首批批次记录（含正常/临期/过期三种状态）

### 4.3 证据三：从多维表格读取数据验证

```
lark-cli base +record-list --base-token W6SAbbkXFaOUV1sWCqAcxl2ynSQ \
  --table-id tblEoHP6EPL63Wmy --as user
```

返回记录字段完整（产品名称、品牌、品类、风险等级默认、飞书文档链接等），数据与写入一致。

### 4.4 证据四：导入飞书知识库文档

```
lark-cli drive +import --file "知识库/xxx.md" --type docx \
  --folder-token K64mfxVq5lxVKXdDynicTwWcn5g
```

首批产品知识 Markdown 已导入为飞书云文档（知识库持续扩充），置于文件夹「美妆零售知识库-产品知识」：
https://guanghe-studio.feishu.cn/drive/folder/K64mfxVq5lxVKXdDynicTwWcn5g

| 产品 | 云文档 token |
|------|------|
| The Ordinary α-熊果苷精华 | JcHAdMF6yoMWWHxYR8Wcj2oznIb |
| The Ordinary 烟酰胺10%+锌 | ZBCkdEPGRo5ixrx7ATcc3CcGnxb |
| 宝拉珍选 2% BHA | G3cbdwd3JoRCjYxJT0UcbgVznf6 |
| 珀莱雅防晒 SPF50 PA++++ | XUbodn2U5oNE0vxNAaicmDSYnse |
| CATHERINEY 卸妆膏 | FSFcdjCFzo0g7FxE2OHc8MF3nNh |

---

## 五、安全设计说明

> 数据安全和日志审计不硬性要求，聚焦跑通业务流程。以下点到即止。

| 维度 | 设计 |
|------|------|
| 5.1 身份认证 | 飞书 user/bot 双身份；CLI 调用走 user 身份 OAuth，token 存 keyring；bot 未授 base 写权限 |
| 5.2 最小权限 | 知识库（云文档 + 商品/效期表）只读；仅会话标记表可写；按需分批授予 base scope |
| 5.3 敏感数据 | 成分/孕期/过敏相关回答不外传个人画像；user_ctx 仅本次会话用，不落库 |
| 5.4 密钥管理 | 飞书 token / LLM API key 走环境变量与 keyring，不进仓库（.gitignore 已排除） |
| 5.5 日志审计 | 本期未硬性要求；CLI 命令执行留有输出可追溯，后续可加结构化日志 |
| 5.6 异常处理 | 检索为空→coverage=false→建议转人工；飞书不可达→本地知识库兜底；LLM 超时→兜底提示 |
| 5.7 人工兜底 | 两级转人工：①AI 标记高风险→建议用户转人工 ②初级客服无法处理→转资深员工；资深员工标记留档 |

---

## 六、总结

### 6.1 交付 checklist

| 交付项 | 状态 |
|------|:---:|
| 系统架构 | ✅ |
| 数据来源和去向 | ✅ |
| 输入输出字段 | ✅ |
| 现有系统接入方式 | ✅（飞书 CLI） |
| MCP/API/CLI 选择依据 | ✅ |
| 飞书云文档/多维表格模拟企业系统 | ✅ |
| 运行证据 | ✅（4 项） |
| 安全设计（身份认证/最小权限/敏感数据/密钥/异常/人工兜底） | ✅ |

### 6.2 飞书资源汇总

| 资源 | 标识 | 链接 |
|------|------|------|
| 产品知识文件夹 | K64mfxVq5lxVKXdDynicTwWcn5g | /drive/folder/K64mfxVq5lxVKXdDynicTwWcn5g |
| 企业系统数据库（多维表格） | W6SAbbkXFaOUV1sWCqAcxl2ynSQ | /base/W6SAbbkXFaOUV1sWCqAcxl2ynSQ |
| · 商品信息表 | tblEoHP6EPL63Wmy | 产品主数据 |
| · 库存效期表 | tblicHw9gxC2nS4M | 批次/效期 |
| · 会话标记表 | tblpRpbprhZIzlqp | 工单留档 |

> 均位于租户 guanghe-studio.feishu.cn。多维表格、交付文档及各产品云文档均已设为「互联网上获得链接的人可查看」，供评委审查。

---

## 附录：CLI 命令速查

| 业务动作 | 命令 |
|---------|------|
| 检索产品知识云文档 | `lark-cli doc` / `lark-cli drive +preview --file <token>` |
| 查商品信息 | `lark-cli base +record-search --base-token <base> --table-id tblEoHP6EPL63Wmy` |
| 查库存效期 | `lark-cli base +record-search --base-token <base> --table-id tblicHw9gxC2nS4M` |
| 标记会话留档 | `lark-cli base +record-batch-create --base-token <base> --table-id tblpRpbprhZIzlqp --json '{...}'` |
| 检索历史案例 | `lark-cli base +record-search --base-token <base> --table-id tblpRpbprhZIzlqp` |
| 导入 Markdown 为云文档 | `lark-cli drive +import --file <md> --type docx --folder-token <folder>` |
